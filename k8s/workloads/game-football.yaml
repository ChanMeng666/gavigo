apiVersion: apps/v1
kind: Deployment
metadata:
  name: game-football
  namespace: gavigo
  labels:
    app: game-football
    type: game
    theme: football
spec:
  replicas: 0  # Starts COLD
  selector:
    matchLabels:
      app: game-football
  template:
    metadata:
      labels:
        app: game-football
        type: game
        theme: football
    spec:
      containers:
        - name: game
          image: registry.digitalocean.com/gavigo-registry/game-football:latest
          ports:
            - containerPort: 8080
          env:
            # Background stress simulation configuration
            - name: STRESS_ENABLED
              value: "true"
            - name: STRESS_CPU_WORKERS
              value: "1"
            - name: STRESS_CPU_LOAD
              value: "20"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: game-football
  namespace: gavigo
spec:
  selector:
    app: game-football
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
