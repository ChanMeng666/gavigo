apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  namespace: gavigo
  labels:
    app: ai-service
    type: ai-service
    theme: tech
spec:
  replicas: 0  # Starts COLD
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
        type: ai-service
        theme: tech
    spec:
      containers:
        - name: ai
          image: registry.digitalocean.com/gavigo-registry/ai-service:latest
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: gavigo
spec:
  selector:
    app: ai-service
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
