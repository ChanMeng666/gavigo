apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: gavigo
  labels:
    app: orchestrator
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: orchestrator
